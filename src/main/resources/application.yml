spring:
  datasource:
    url: jdbc:h2:mem:holiday_master_central
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect